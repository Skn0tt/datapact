import { BeNormal } from "./BeNormal"
import { ExpectationVisualiser } from "./ExpectationVisualiser"

const list: Record<string, ExpectationVisualiser> = {
  be_normal: BeNormal,
}

export default list
